# 車両検出カウントプログラム
# 概要

# 工夫点
# 使用技術
# 動作方法
# AIについて

// 必要なインストール
pip install flask
pip install opencv-python
pip install ultralytics
pip install openvino

//　付属のopenh264を　py -c "import sys; print(sys.base_prefix)"  で表示されるフォルダに追加
// app.pyをターミナルで実行したらウェブが開く　
// サンプル映像として0-10.mp4を同封しています 